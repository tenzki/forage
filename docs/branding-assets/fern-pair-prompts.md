# Four- and five-pair botanical illustrations

Mode: built-in image generation. Reference: fern-iteration-06-simple.png (style and perspective).

## 4 pairs

Asset: fern-four-pairs.png

Use case: logo-brand. Create one new simplified botanical branch illustration matching the supplied image's square-dot style, colors and diagonal three-quarter perspective. The input image is a STYLE reference. Critical requirement: EXACTLY 4 opposite pairs of leaves: 8 side leaves total, 4 leaves on each side, paired at 4 distinct, well-separated nodes along ONE slender curved stem. Each leaf is a simple smooth elongated almond shape, with no subsidiary branches, no serrations and no subdivisions. Count all 4 pairs before finishing. No unpaired terminal leaf. The stem starts at lower left and sweeps diagonally up toward upper right with a gentle arc; far-side leaves angle upward, near-side leaves extend rightward. Smallest pair near tip, largest near lower middle, plenty of air between pairs. End the stem with only a tiny violet dot bud, NOT a leaf. Entire illustration composed of crisp evenly spaced separate axis-aligned square dots, constant square size, regular visible gaps. Forest #203D32, moss #64735B at outer leaf edges, tiny violet #7057A3 tip. Flat warm ivory #F5F2E8 background. Whole branch fully visible with generous margins. No text, numbers, labels, decorative extras, shadows, grain or gradients. Simple and restrained, same visual family as reference.

## 5 pairs

Asset: fern-five-pairs.png

Use case: logo-brand. Create one new simplified botanical branch illustration matching the supplied image's square-dot style, colors and diagonal three-quarter perspective. The input image is a STYLE reference. Critical requirement: EXACTLY 5 opposite pairs of leaves: 10 side leaves total, 5 leaves on each side, paired at 5 distinct, well-separated nodes along ONE slender curved stem. Each leaf is a simple smooth elongated almond shape, with no subsidiary branches, no serrations and no subdivisions. Count all 5 pairs before finishing. No unpaired terminal leaf. The stem starts at lower left and sweeps diagonally up toward upper right with a gentle arc; far-side leaves angle upward, near-side leaves extend rightward. Smallest pair near tip, largest near lower middle, plenty of air between pairs. End the stem with only a tiny violet dot bud, NOT a leaf. Entire illustration composed of crisp evenly spaced separate axis-aligned square dots, constant square size, regular visible gaps. Forest #203D32, moss #64735B at outer leaf edges, tiny violet #7057A3 tip. Flat warm ivory #F5F2E8 background. Whole branch fully visible with generous margins. No text, numbers, labels, decorative extras, shadows, grain or gradients. Simple and restrained, same visual family as reference.

